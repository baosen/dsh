#include "do.hpp"

// Click on window at position p.
void Do::click(const pos& p) 
{
    // Is outside window rectangle?
    if (p.x < pcur.x || p.y < pcur.y)
        return;
}

// Do an action.
int main() 
{
    // First program ran used to load and execute other programs.
    Do r;
}
